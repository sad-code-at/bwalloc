# Core — required
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
xgboost>=2.0            # 2.0+ needed for objective="reg:quantileerror"

# Optional — the benchmark picks these up automatically if present
lightgbm>=4.0
statsmodels>=0.14       # STL, ARIMA, Holt-Winters comparisons in the audit notebook

# Optional — deep sequence models (CNN/LSTM/GRU comparisons). Nothing in this
# repository currently uses torch: every model here is scikit-learn or XGBoost and
# runs on CPU. bwalloc.set_seed() seeds torch only if it happens to be installed.

# Optional — zero-shot time-series foundation model baselines.
# Needed only by experiments/run_foundation.py. Pulls in torch and transformers;
# runs on CPU (Chronos-Bolt Small is ~48M parameters against 900-point series).
# The runner handles both the 1.x `context=` and 2.x `inputs=` argument names.
chronos-forecasting>=2.0
# timesfm>=1.2          # heavier; not used here

# Development
pytest>=7.4
